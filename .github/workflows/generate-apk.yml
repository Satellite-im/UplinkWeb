name: Generate Android APK
on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🔖
        uses: actions/checkout@v4.1.7

      - name: Setup Node.js 🔨
        uses: actions/setup-node@v4.0.4
        with:
          node-version: 20

      - name: Install Dependencies 🔨
        run: npm i
        
      - name: Build Web Assets 🔨
        run: npm run build
        
      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v2
    
      - name: Sync Capacitor Assets 🔨
        run: npx cap sync && npx cap copy android && cd android && ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk

      - name: Post APK download link
        if: success()  # Only run if the build and upload succeed
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          header: APK Download
          message: |
            Download the [APK artifact](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifact_debug-apk) for testing.

      - name: Post APK download link comment
        if: success()  # Only run if the build and upload succeed
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          header: nightly-link
          message: |
            Download the .APK for this pull request:
            * [debug-apk.zip](${{ steps.get_artifact.outputs.apk_url }})
