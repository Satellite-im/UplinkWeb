<div class="controls">
    <slot></slot>
</div>

<style lang="scss">
    .controls {
        display: inline-flex;
        min-width: fit-content;
        height: fit-content;
        gap: var(--gap-less);

    }
</style>